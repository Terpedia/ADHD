#!/bin/sh

set -e

if command -v python3 >/dev/null 2>&1; then
  echo "Running Mermaid syntax checks..."
  python3 scripts/check_mermaid.py
else
  echo "python3 not found; skipping Mermaid syntax checks." >&2
fi
